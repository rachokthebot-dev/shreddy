services:
  practicepad:
    build: .
    ports:
      - "3000:3000"
    volumes:
      - practicepad-data:/app/data
    environment:
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY:-}
    restart: unless-stopped

volumes:
  practicepad-data:
